{{!-- 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="All skills" big=true}}
            <ul>
                {{#each skills}}
                <li>
                    {{name}}
                    {{#if points includeZero=true}} ({{points}}) {{/if}}
                    {{#if isTop}} (Top) {{/if}}
                    {{#unless points includeZero=true}} (не вивчаємо) {{/unless}}
                    {{#unless points}} 
                        {{#if points includeZero=true}}
                            (зараз вивчаємо!!!) 
                        {{/if}}
                    {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>

        <section>
            {{> title data="My hobbies" big=false}}
            <ol>
                {{#each hobbies}}
                <li>
                    {{name}}
                    {{#if isMain includeZero=true}} (favourite) {{/if}}

                </li>
                {{/each}}
            </ol>
        </section>
    </main>
    {{/with}}
</div>

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}
