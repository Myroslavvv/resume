<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="My educationss" big=true}}
            <ul>
                {{#each educations}}
                <li>
                    {{name}} ({{points}})

                    {{#if isEnd}} 
                        (End) 
                    {{else}}
                        (ще в процесі)
                    {{/if}}
                </li>
                {{/each}}
            </ul>
        </section> 

        <section>
            {{> title data="My certificates" big=false}}
            <ul>
                {{#each certificates}}
                <li>
                    {{name}} ({{id}})
                </li>
                {{/each}}
            </ul>
        </section>

       
    </main>
    {{/with}}
</div>

